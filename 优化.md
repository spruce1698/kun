# 代码审查报告：Bug 与优化建议

> 审查范围：`main.go` / `cmd` / `internal` / `pkg` / `tpl/advanced` / `tpl/basic`
> （`advanced.zip` 与 `basic.zip` 内容分别与 `tpl/advanced`、`tpl/basic` 一致，未解压，仅审查目录源码；`*_gen.go`、`wire_gen.go` 为生成代码，一般不在审查范围。）
> 审查依据：Go 官方最佳实践、Gin 官方推荐用法、GORM / go-redis / kafka-go / zap 等库的正确用法，以及常见安全与并发陷阱。
>
> 说明：basic 与 advanced 模板的 `pkg/*` 代码除 import 前缀与少量裁剪外基本一致，下文以 advanced 路径标注，同类问题在 basic 中同样存在，不再重复。

---

## 一、Bug（功能性错误，应优先修复）

### 1. AES CTR/OFB/CFB 流模式错误地做 PKCS7 填充
- 文件：[tpl/advanced/pkg/encrypt/encrypt.go](tpl/advanced/pkg/encrypt/encrypt.go)
- 问题：CTR、OFB、CFB 均为**流密码模式**，明文长度 = 密文长度，不需要（也不应该）做块填充。但代码对 CTR/OFB 加密时调用了 `PKCS5Padding`，解密时调用 `PKCS5UnPadding`：
  - `AESEncryptCTR` / `AESDecryptCTR`（L298-368）
  - `AESEncryptOFB` / `AESDecryptOFB`（L372-438）
  - `DESEncryptCTR` / `DESDecryptCTR`、`DESEncryptOFB` / `DESDecryptOFB`、`DES3EncryptCTR` / `DES3DecryptCTR`、`DES3EncryptOFB` / `DES3DecryptOFB` 同样问题
  - （`CFB` 没有做填充是对的，反而说明 CTR/OFB 是漏改）
- 后果：
  - 加密时多塞一段填充字节，解密时按"最后一字节"截断；若密文被篡改或解密端实现不同，会**截断真实数据**或解出乱码。
  - 与其它语言/库的 CTR 互通必然失败（对端不会去 padding）。
- 建议：CTR/OFB 不做 padding，直接 `XORKeyStream(密文, 明文)`；解密同样直接 `XORKeyStream(明文, 密文)`。

### 2. `intToBase` 对 `math.MinInt64` 仍会溢出，注释与实现不符
- 文件：[tpl/advanced/pkg/utils/convert.go:14-20](tpl/advanced/pkg/utils/convert.go#L14-L20)
- 问题：注释声称"转 uint64 取绝对值，避免 int64 直接 -n 溢出"，但实际写的是 `u = uint64(-n)`，当 `n = math.MinInt64` 时 `-n` 仍是 `MinInt64`（溢出），`uint64(-n)` 得到的是 `9223372036854775808`，结果**不正确**。
- 建议：直接 `u := uint64(n)` 后按无符号取绝对值，或 `if n < 0 { neg = true; u = ^uint64(n) + 1 }`（即 `-uint64(n)` 的无符号运算），可正确处理 `MinInt64`。

### 3. `GenStr` 未校验 length，负值会 panic
- 文件：[tpl/advanced/pkg/utils/random.go:19-51](tpl/advanced/pkg/utils/random.go#L19-L51)
- 问题：`b.Grow(length)` 在 `length < 0` 时会 panic；`length == 0` 虽不 panic 但当前调用方都传正数。作为通用工具函数应做防御。
- 建议：开头加 `if length <= 0 { return "" }`。

### 4. `Convert` 字段名 `Test1` 在 `db.Demo` 是 `float64`，但 `demo.Demo` 也用 `float64`，`copier.Copy` 把金额用 float64 承载——精度风险（功能性隐患）
- 文件：[tpl/advanced/internal/repository/db/demo_gen.go:49](tpl/advanced/internal/repository/db/demo_gen.go#L49)、[tpl/advanced/internal/service/svc/demo.go:86](tpl/advanced/internal/service/svc/demo.go#L86)
- 问题：`test1` 为 `decimal(10,2)`，生成类型为 `float64`。金额用 float64 运算/传输存在精度丢失，与同仓 `utils/decimal` 提供的"元转分"思路矛盾。
- 建议：`genDBRepo` 的 `dataTypeMap` 中 `decimal` 默认映射为 `float64`，至少应在文档/示例里提示金额字段建议手动改为 `decimal.Decimal` 或存为整数（分）。这是模板的"示范"问题，影响所有用户新建项目。

### 5. `jwt.Refresh` 绑定校验用错了 claims 的 `IssuedAt`
- 文件：[tpl/advanced/pkg/token/jwt.go:185](tpl/advanced/pkg/token/jwt.go#L185)
- 问题：`Gen` 中 refreshToken 的 `ID = MD5to16(accessClaims.ID + Subject + Issuer + accessClaims.IssuedAt.String())`（用的是 access 的 IssuedAt）。而 `Refresh` 中校验：
  ```go
  encrypt.MD5to16(accessClaims.ID+accessClaims.Subject+accessClaims.Issuer+refreshClaims.IssuedAt.String()) != refreshClaims.ID
  ```
  用的是 `refreshClaims.IssuedAt`。虽然 `Gen` 时 `refreshClaims.IssuedAt = accessClaims.IssuedAt`（同值），当前能通过；但这是**隐式依赖两个值相等**的脆弱写法。一旦后续修改让两者不再同值（例如 refresh 改签发时间），校验会立即失效或误判。
- 建议：统一用一个固定来源（如始终用 `accessClaims.IssuedAt`）做绑定，并在注释中说明绑定字段，避免后续改动引入静默 bug。

### 6. `wire2DI.go` 幂等去重逻辑会误判"已存在"而漏写
- 文件：[tpl/advanced/internal/create/kernel/wire2DI.go:82-87](tpl/advanced/internal/create/kernel/wire2DI.go#L82-L87)
- 问题：去重判断对 `appendContent` 按行 split，只要**任意一行**的 trim 结果已存在于文件中就 `return nil` 跳过。但插入内容常含多行（如 service 的 diBuilder 返回两行：`wire.Struct(...)` 和 `NewXxxSvc,`）。若文件里恰好已有其中一行（例如另一个 svc 共用了 `wire.Struct(new(xxx.Ctx), "*")` 这类通用行，或 import 行），会**误判已插入**而漏掉真正该新增的行。
- 后果：多次 `create svc` 后，部分 New 函数未注册进 WireSet，wire 生成失败或运行期缺依赖。
- 建议：改为"整段 `appendContent`（trim 后）是否作为连续片段出现"判断，或基于"marker 行下方紧跟的行"精确比对，而非逐行任一命中。

### 7. `create.go` 中非默认包名时给两个不同 marker 写同一个 import 行，逻辑可疑
- 文件：[tpl/advanced/internal/create/create.go:325-328](tpl/advanced/internal/create/create.go#L325-L328)
- 问题：
  ```go
  if c.PackageName != config.defaultPkg {
      contentMap["github.com/google/wire"] = "\t\"" + c.ProjectName + "/" + filePath + "\""
      contentMap["// ==== Add Rt import  before this line, don't edit this line.===="] = "\t\"" + c.ProjectName + "/" + filePath + "\""
  }
  ```
  - `"github.com/google/wire"` 这个 key 本身不是 marker 文本，作为 contentMap 的 key 没有对应 marker 行，`findDIFiles` 不会基于它匹配，`wireProcess` 里也找不到该 marker 行（除非文件里真有字面量 `github.com/google/wire`，那会误匹配 import 区任意位置）。该 key 实际只在"是否含任一 marker"判断时被遍历，行为不可控。
  - 对所有类型（handler/service/cache）都塞 `Add Rt import` marker，但该 marker 只在 router DI 文件存在；其它 DI 文件没有这个 marker，于是 `wireProcess` 找不到 marker 行而 `return nil`，看似无害但掩盖了"本应加 import"的场景。
- 建议：import 注入应针对各类型对应的真实 import marker 单独处理，且 marker 文本不要用 `github.com/google/wire` 这种字面量做 key。建议为每种 DI 文件定义独立的 import marker 注释行。

### 8. `run_common.go` 在 `cmdExit` 分支直接 `return`，文件 watcher 期间未消费 `cmdExit` 可能导致重启后误触发
- 文件：[tpl/advanced/../internal/run/run_common.go:179-187](internal/run/run_common.go#L179-L187)
- 问题：`case err := <-cmdExit:` 收到子进程退出后直接 `return` 退出 watch。但在"文件修改重启"路径里，重启后新建了 `cmdExit = make(...)`，旧 `cmdExit` 已被消费；这里逻辑尚可。真正问题是 `start()` 里的 `buildProbeDelay` 期间若进程已退出，`start` 内部会 `cmd.Wait()` 一次，而 watch 主循环里又 `go func() { cmdExit <- cmd.Wait() }()`——**同一个 cmd 被两个地方 Wait**。
  - `start()` 内：编译失败时 `cmd.Wait()`（L253）。
  - watch 里：`go func() { cmdExit <- cmd.Wait() }()`（L130/L165）。
  - 若 `start()` 内已 `Wait()` 成功（返回 nil 给调用方，cmd 存活），watch 的 goroutine 再 `Wait()` 是允许的（go 允许多次 Wait 返回同一 err），但若 `start()` 内 Wait 拿到错误后 `return nil` 之外的路径……实际 `start` 在编译失败时返回 nil cmd，watch 里 `if cmd != nil` 才起 goroutine，所以当前没 double-Wait 同一 cmd 的崩溃。但这是**脆弱的隐式约束**，靠"start 返回 nil 时不起 goroutine"勉强成立。
- 建议：在注释里固化该约束，或让 `start` 不做 `Wait`，统一由 watch 的 goroutine Wait 并通过 channel 传递编译错误，消除"两个地方都可能 Wait"的隐患。

### 9. `http.Server` 未设置 `ReadHeaderTimeout`，存在 Slowloris 慢速攻击风险
- 文件：[tpl/advanced/pkg/xserver/http/http.go:142-147](tpl/advanced/pkg/xserver/http/http.go#L142-L147)、[tpl/advanced/pkg/xserver/broker/broker.go:262-267](tpl/advanced/pkg/xserver/broker/broker.go#L262-L267)
- 问题：`http.Server` 只设了 `ReadTimeout`/`WriteTimeout`，未设 `ReadHeaderTimeout`。Go 官方安全建议（gov.uk、Go 官方文档）明确推荐设置 `ReadHeaderTimeout` 以防御 Slowloris。`ReadTimeout` 涵盖整个请求体读取，对 streaming/SSE/大上传会被误杀，而 `ReadHeaderTimeout` 专门保护读 header 阶段。
- 建议：`ReadHeaderTimeout: 10 * time.Second`（按需调整），并复核 SSE/导出接口的 `WriteTimeout=0` 是否与整体 `WriteTimeout=120s` 冲突（见优化项 18）。

### 10. `xconfig` 热重载后，`Mysql/Redis/Kafka` 等连接字段不重建，但 `Server.Port/Timeout` 等运行期读取的字段未通过 `Get()` 读取
- 文件：[tpl/advanced/pkg/xconfig/xconfig.go:28-31](tpl/advanced/pkg/xconfig/xconfig.go#L28-L31)、[tpl/advanced/pkg/xserver/http/http.go:131-136](tpl/advanced/pkg/xserver/http/http.go#L131-L136)
- 问题：注释明确说"运行期重复读取必须走 `Get()`，否则拿不到热重载后的值"。但 `Server.Start` 里直接 `s.Conf.Server.ReadTimeout`，`http.go` 的 `New` 里直接 `conf.Env`、`conf.Server.AllowOrigins` 等。这些是启动期一次性读取，**本身没问题**；但 `CORS` 中间件在闭包里固定了 origins（启动期快照），热重载 allowOrigins 后不会生效——这与注释承诺的"热重载即时生效"存在认知落差。
- 建议：要么明确文档"哪些字段支持热重载（仅 Env 等通过 Get() 读取的）"，要么把 CORS 白名单等也改为每次请求读 `conf.Get()`。当前实现合理，但文档/注释易误导。

### 11. `ratelimit` 在锁内调用 `ctx.ClientIP()` 之前的逻辑——`ClientIP()` 在锁外，但 `banned`/`records` 清理协程删除的是"最旧一半"用 map 随机遍历
- 文件：[tpl/advanced/internal/middleware/ratelimit.go:50-60](tpl/advanced/internal/middleware/ratelimit.go#L50-L60)
- 问题：`if len(records) > maxRateLimitEntries { half := len(records)/2; for ip := range records { delete... } }`——Go map 遍历顺序随机，删的并非"最旧一半"，注释"强制清理最旧的一半"**描述与实现不符**。
- 建议：要么改成按 `firstSeen` 排序后删最旧的，要么把注释改为"随机删除一半以控制规模"。

### 12. `subscriber.go` 的 `Sub`（自动提交）路径在 `break` 后未关闭 reader，但 Close 统一处理；而 `SubFetch` 用 `fmt.Printf` 打日志
- 文件：[tpl/advanced/pkg/kafka/subscriber.go:59](tpl/advanced/pkg/kafka/subscriber.go#L59)、[tpl/advanced/pkg/kafka/subscriber.go:92](tpl/advanced/pkg/kafka/subscriber.go#L92)、[tpl/advanced/pkg/kafka/subscriber.go:105](tpl/advanced/pkg/kafka/subscriber.go#L105)
- 问题：
  - 消费错误用 `fmt.Printf` 直接打 stdout，绕过了 `xlog`，生产环境日志收集不到。
  - `consumeWithRetry` 的 retry 也用 `fmt.Printf`。
  - 同样 `broker.go` 的 `childProcess` 里 `fmt.Printf("!!!BrokerErr!!! ...")`。
- 建议：注入 logger，统一走 `xlog`。

### 13. `broker.go` 的 `supervisor` 用 `os.Args` 作为子进程参数，但 `os.Args[0]` 在 `go run` 下是临时编译产物
- 文件：[tpl/advanced/pkg/xserver/broker/broker.go:97-104](tpl/advanced/pkg/xserver/broker/broker.go#L97-L104)
- 问题：`Path: os.Args[0], Args: os.Args`。开发期用 `kun run` → `go run` 启动时，`os.Args[0]` 是 `go` build 出的临时二进制路径，子进程再次 exec 该路径通常可行；但若父进程是 `go run main.go`，`os.Args` 还含 `run main.go` 等参数，子进程会再次 `go run`，行为可能与预期不符。生产 `go build` 后正常。
- 建议：文档明确 broker 多进程模式仅在编译后二进制运行；或提供单进程开关便于开发。

### 14. `broker.go` `supervisor` 中 `cmd := exec.Cmd{...}` 取地址 `&cmd` 存到 `s.curCmd`，但 `cmd.Wait()` 后 cmd 仍在闭包引用，Stop 里读 `cmd.Process` 可能在 Wait 后访问
- 文件：[tpl/advanced/pkg/xserver/broker/broker.go:111-182](tpl/advanced/pkg/xserver/broker/broker.go#L111-L182)
- 问题：`cmd.Wait()` 返回后 `cmd.Process` 仍非 nil（Process 在 Wait 后保留，但 `ProcessState` 已设）。Stop 里 `cmd.Process.Signal(SIGTERM)` 给一个已退出的进程发信号，syscall 会返回错误但被 `_ =` 忽略，随后 `select { case <-s.cmdExit: }`——但 `s.cmdExit` 可能在 supervisor 里因 `select { case s.cmdExit <- waitErr: default: }` 已经满了（cap=1）而没投递成功，导致 Stop 的 `<-s.cmdExit` 永久阻塞直到 5s 超时 kill。
  - 场景：子进程自己退出（非 Stop 触发），supervisor 把 waitErr 投进 cmdExit（成功，因为此时 cmdExit 空），supervisor 随后 `return`。此时若 Stop 被调用，`<-s.cmdExit` 立刻拿到值，OK。但若 supervisor 还没投递就 `return` 了（投递失败走 default），Stop 的 `<-s.cmdExit` 会阻塞到 5s 超时。
- 建议：Stop 等待子进程退出用独立机制（如 `cmd.Wait()` 已被 supervisor 调用，可用 `Process.Wait` 不可重复；建议用 `done chan struct{}` 由 supervisor 关闭来表示子进程已退出），避免依赖 cmdExit 的投递时序。

### 15. `xdb.New` 用 `log.New(os.Stdout, ...)` 作为 gorm logger writer，绕过 xlog
- 文件：[tpl/advanced/pkg/xdb/xdb.go:36-43](tpl/advanced/pkg/xdb/xdb.go#L36-L43)
- 问题：`initLog(log.New(os.Stdout, "\n", log.LstdFlags), ...)` 把 gorm 日志写到 stdout，但 `xdbLogger` 的 `Info/Warn/Error/Trace` 实际调用 `xlog.Info(...)`（见 xdblog.go），`Writer` 字段只是存了没用上的 fallback。这里 `log.New(os.Stdout,...)` 创建后从未被真正用于输出（xdbLogger 重写了所有方法走 xlog），属于死代码 + 误导。
- 建议：移除 `log.New(os.Stdout,...)`，`initLog` 的 writer 参数若不需要可简化；或保留但注释说明用途。

### 16. `xdblog.go` `Trace` 的 Error 分支条件逻辑反人类
- 文件：[tpl/advanced/pkg/xdb/xdblog.go:85](tpl/advanced/pkg/xdb/xdblog.go#L85)
- 问题：`err != nil && l.LogLevel >= logger.Error && (!errors.Is(err, logger.ErrRecordNotFound) || !l.IgnoreRecordNotFoundError)`。当 `IgnoreRecordNotFoundError=true`（xdb.New 里恒为 true）时，`!l.IgnoreRecordNotFoundError` 恒 false，条件退化为 `!errors.Is(err, ErrRecordNotFound)`——即非 NotFound 的错误才记录，正确。但表达式可读性差，且 `IgnoreRecordNotFoundError` 在运行期不可变，分支里再判一次意义不大。
- 建议：简化为 `err != nil && l.LogLevel >= logger.Error && !(errors.Is(err, logger.ErrRecordNotFound) && l.IgnoreRecordNotFoundError)`，语义更清晰。

### 17. `xredis.New` 在 cluster 模式下只 Ping 单节点逻辑、且 `conf.Redis.Source[0]` 在错误信息里假设单机
- 文件：[tpl/advanced/pkg/xredis/xredis.go:49-58](tpl/advanced/pkg/xredis/xredis.go#L49-L58)
- 问题：`redisotel.InstrumentTracing(client)` 传 `client`（`redis.UniversalClient` 接口），但 `InstrumentTracing` 对 cluster/client 的钩子注册不同；更重要的是错误信息 `redis[%v]` 用 `conf.Redis.Source[0]`，cluster 下首个地址不代表全集群。
- 影响：低，仅日志可读性。
- 建议：cluster 模式错误信息打印全部 addrs。

### 18. SSE/导出接口清除了 `WriteDeadline`，但 `http.Server.WriteTimeout=120s` 仍会在 120s 后切断连接
- 文件：[tpl/advanced/internal/handler/demo/demo.go:345-356](tpl/advanced/internal/handler/demo/demo.go#L345-L356)、[tpl/advanced/pkg/xserver/http/http.go:129-147](tpl/advanced/pkg/xserver/http/http.go#L129-L147)
- 问题：handler 用 `http.NewResponseController(ctx.Writer).SetWriteDeadline(time.Time{})` 清除写超时，但 `http.Server.WriteTimeout` 是在 `http.Server` 层设置的，对整个连接生效；`SetWriteDeadline` 只能缩短/调整，无法越过 `Server.WriteTimeout`（实际上 `ResponseController.SetWriteDeadline` 在 net/http 标准库里会覆盖 server 配置，对当前 request 生效——这一点依赖 Go 1.20+ 的实现）。需确认运行 Go 版本 ≥1.20（ResponseController 是 1.20 引入）。
- 建议：在 go.mod 确认 `go 1.20+`；并对长连接接口（SSE/ws/export）路由单独处理，或干脆对这类 handler 不设全局 WriteTimeout（`WriteTimeout=0`），改用 `ReadHeaderTimeout` + per-handler deadline。当前全局 120s WriteTimeout 会在长导出场景切断流。

### 19. `xlog.Errorf` 等函数签名与 `Error` 不一致，`Error(ctx, message, err, params)` 但 `Errorf(ctx, message, params)` 无 err 参数
- 文件：[tpl/advanced/pkg/xlog/xlog.go:172-190](tpl/advanced/pkg/xlog/xlog.go#L172-L190)
- 问题：`Error` 接受 `(ctx, message, err, params...)`，`Errorf` 接受 `(ctx, message, params...)` 无 err。调用方 `xlog.Errorf(ctx, "错误代码:%4s %v", errCode(e.err), e.err)`（xerror/error.go:71）靠 `params` 传 err，而 `Error` 单独传 err 塞进 `content.Error`。两套 API 不对称，易误用：调用 `Errorf` 时 err 不会进入结构化 `error` 字段，只出现在格式化 message 里，影响日志检索。
- 建议：统一 API，`Errorf` 也接受 err，或全部用结构化字段。

### 20. `xlog.fromContext` 在 ctx 无 logger 时回退 `zap.L()`，但 `zap.L()` 在 `New` 之前调用得到的是默认 logger
- 文件：[tpl/advanced/pkg/xlog/xlog.go:234-239](tpl/advanced/pkg/xlog/xlog.go#L234-L239)
- 问题：`New` 里 `zap.ReplaceGlobals(logger)` 替换全局。但若有任何包在 `xlog.New` 之前调用 `xlog.Info`（初始化顺序问题），`zap.L()` 是 zap 的默认无配置 logger，输出到 stderr。属于初始化时序隐患。
- 建议：确保 xlog.New 是最优先初始化的依赖（wire 里放最前），或在 `fromContext` 回退时若全局未初始化则丢弃日志并告警。

### 21. `validator.New` 用 `if Trans == nil` 包裹 `once.Do`，导致换 locale 重新调用不生效
- 文件：[tpl/advanced/pkg/validator/validator.go:41-83](tpl/advanced/pkg/validator/validator.go#L41-L83)
- 问题：`once.Do` 只执行一次，且外层 `if Trans == nil` 在第二次调用时 `Trans` 已非 nil 直接跳过——即 locale 参数只在首次生效。若不同服务实例想用不同 locale，无法切换。
- 影响：低，通常一次性初始化。
- 建议：可接受，但应删除冗余的 `if Trans == nil` 判断（`once.Do` 已保证），或支持显式重新注册。

### 22. `cache.demoCache.Get` 本地缓存命中时返回的指针可能被调用方修改，污染缓存
- 文件：[tpl/advanced/internal/repository/cache/demo.go:61-66](tpl/advanced/internal/repository/cache/demo.go#L61-L66)
- 问题：`localCache.Get` 返回的是存入的 `*Demo` 指针本身（go-cache 存的是 any 引用）。`demoSvc.Detail` 里 `_ = copier.Copy(result, demo)` 把 demo 拷到新 result，不污染——但若有调用方直接修改返回的 `*Demo`，会改到缓存里的对象。属于"返回可变引用"的通用陷阱。
- 建议：本地缓存返回值时深拷贝，或在文档明确"缓存返回对象不可变"。

### 23. `cache.demoCache.Set` 未同步写本地缓存
- 文件：[tpl/advanced/internal/repository/cache/demo.go:86-103](tpl/advanced/internal/repository/cache/demo.go#L86-L103)
- 问题：`Set` 只写 Redis，不写 localCache；而 `Get` 是 local → redis。`demoSvc.Detail` 在 db 回填时调用 `DemoCache.Set` 后，下次请求 `Get` 仍会 local miss → redis hit → 再回填 local。功能正确但多一次 redis 往返。
- 建议：`Set` 时同步写 local（`d.localCache.Set(key, data, ttl)`），与 redis 保持一致。

### 24. `demoSvc.Detail` 缓存了不完整对象
- 文件：[tpl/advanced/internal/service/svc/demo.go:129-134](tpl/advanced/internal/service/svc/demo.go#L129-L134)
- 问题：回填缓存时构造的 `cache.Demo` 只有 `Id/Name/Test4/RoleId` 四个字段，缺 `Test1` 等。而 `cache.Demo` 结构体定义也确实只有这 4 个字段（cache/demo.go:36-43）。但 `result` 是从 `demoDb`（完整 db.Demo）copier.Copy 来的，返回完整对象——**缓存内容与返回内容不一致**。下次缓存命中时 `copier.Copy(result, demo)` 只能拷 4 个字段，`Test1` 等会丢失/为零值。
- 后果：第二次请求 Detail 返回的 `test1` 等字段会变成零值，数据错误。
- 建议：`cache.Demo` 与回填字段对齐业务需要，或直接缓存完整 db.Demo（注意 Password 字段不能进缓存）。

### 25. `demoSvc.Update` 失效缓存 `args.Id-1`，依赖 `UpdateTrans` 内部删 id-1 的副作用
- 文件：[tpl/advanced/internal/service/svc/demo.go:221-222](tpl/advanced/internal/service/svc/demo.go#L221-L222)
- 问题：注释说"UpdateTrans 内部会删除 id-1 的记录，故 args.Id 与 args.Id-1 的缓存都要失效"。这是 demo 示例的事务（先查 id-1、删 id-1、改 id），生产中不会这么做。但作为模板，把"事务内删相邻 id"作为示范极易被新手复制成坏习惯。
- 建议：换一个更合理的事务示例（如扣减库存 + 写流水），避免误导。

### 26. `handler/demo.go` 的 `WS` 升级失败时 `xhttp.BusFail` 可能写不出响应
- 文件：[tpl/advanced/internal/handler/demo/demo.go:519-539](tpl/advanced/internal/handler/demo/demo.go#L519-L539)
- 问题：`ConsumeWSTicket` 失败时 `xhttp.BusFail` 正常。但 `upgrader.Upgrade` 失败时只 `xlog.Error` 后 `return`，未给客户端任何 HTTP 响应（此时 Upgrade 可能已 hijack 连接或写了 400）。gorilla/websocket 在 Upgrade 失败时会自己写一个 HTTP 错误响应，所以一般 OK，但依赖库行为。
- 建议：确认 gorilla 版本行为，或在 Upgrade 失败后显式 `ctx.Status(http.StatusBadRequest)` 前提是连接未 hijack。

### 27. `upgrader.CheckOrigin` 闭包里用 `fmt.Sprintf` + 立即调用匿名函数，可读性差且 `r.TLS` 判断不够严谨
- 文件：[tpl/advanced/internal/handler/demo/demo.go:510-516](tpl/advanced/internal/handler/demo/demo.go#L510-L516)
- 问题：`origin == scheme://host` 的判断没考虑端口标准化（如 `Host: example.com:443` 与 `Origin: https://example.com` 不等）。浏览器 WS 同源检查建议用 `net/url` 解析比较。
- 建议：用 `url.Parse` 解析 origin 与 host 比较，或直接用 `csrf`/`gorilla` 推荐的 origin 校验。

---

## 二、安全相关

### 28. `xlog.GetClientIP` 直接信任 `X-Real-IP`/`X-Forwarded-For` 等头，可伪造
- 文件：[tpl/advanced/pkg/xlog/http.go:35-61](tpl/advanced/pkg/xlog/http.go#L35-L61)
- 问题：用于日志的 client IP 直接取头，攻击者可伪造。若该 IP 用于风控/限流（`ratelimit` 用的是 `ctx.ClientIP()` 走 gin，相对安全），日志层伪造影响小。但 `utils.ClientIP`（net.go:101）同样问题，且逻辑混乱（先取头，再用 RemoteIP 覆盖，再用 `c.ClientIP()` 覆盖）。
- 建议：日志 IP 统一用 `gin.Context.ClientIP()`（已配置 trusted proxies），删除 `utils.ClientIP` 这个混乱实现。

### 29. JWT 密钥强度无校验，`HS384` 要求 ≥384bit 密钥才安全
- 文件：[tpl/advanced/pkg/token/jwt.go:90-92](tpl/advanced/pkg/token/jwt.go#L90-L92)
- 问题：只校验非空，未校验长度/熵。HS384 最佳实践要求密钥 ≥48 字节（384bit）。
- 建议：启动期校验 `len(secret) >= 32`（至少 256bit）并告警/拒绝弱密钥。

### 30. `AuthFail` 返回 HTTP 201 以绕过 axios 拦截器——非标准做法
- 文件：[tpl/advanced/pkg/xhttp/response.go:54-65](tpl/advanced/pkg/xhttp/response.go#L54-L65)
- 问题：鉴权失败返回 `http.StatusCreated(201)`，语义错误。注释说明是为前端 axios 捕获，但这是把后端语义迁就前端实现，会让监控/网关/重试逻辑误判为成功。
- 建议：返回 401，前端拦截器按业务 code（`xerror.AuthTimeOut` 等）处理，而非靠 HTTP 状态码。

### 31. `CORS` 中间件：`origin == ""` 时设为 `*`，但带凭证时回显 origin——`Access-Control-Allow-Origin: *` 与凭证不兼容
- 文件：[tpl/advanced/internal/middleware/cors.go:27-56](tpl/advanced/internal/middleware/cors.go#L27-L56)
- 问题：当请求无 Origin（如同源请求、curl），设 `origin="*"` 并回 `Access-Control-Allow-Origin: *`，无害。但 OPTIONS 预检时若带了凭证 cookie 又命中 `*` 分支，浏览器会拒绝。整体逻辑可接受，但 `Access-Control-Allow-Headers` 写死一长串，新增头需改代码。
- 建议：可接受；建议从配置读 allowed headers。

### 32. `csrf.Secure(false)` 默认关闭 HTTPS-only，生产需显式开启
- 文件：[tpl/advanced/internal/middleware/csrf.go:21](tpl/advanced/internal/middleware/csrf.go#L21)
- 问题：`csrf.Secure(false)` 使 cookie 在 HTTP 下也传输，生产 HTTPS 应为 true。模板默认 false 适合开发，但应在配置里可控。
- 建议：`csrf.Secure(conf.Env == EnvRelease)` 或从配置读。

### 33. `RSA` 私钥加密/公钥解密（签名式用法）使用 PKCS1v15 而非 OAEP，且自定义 `priKeyEncrypt` 复制标准库内部实现
- 文件：[tpl/advanced/pkg/encrypt/rsaExt.go:210-229](tpl/advanced/pkg/encrypt/rsaExt.go#L210-L229)
- 问题：
  - "私钥加密、公钥解密"实际是签名场景，应直接用 `rsa.SignPKCS1v15` / `rsa.VerifyPKCS1v15`，而非手搓 `priKeyEncrypt`（复制了 crypto/rsa 内部 `decrypt` 等）。手搓大数运算极易出错，且 `decrypt` 里 `rand.Int(random, priv.N)` 用于签名是无意义的（签名不需要盲化）。
  - 加密用 `EncryptPKCS1v15`（PKCS1 v1.5），存在 padding oracle 风险，推荐 `EncryptOAEP`。
- 建议：私钥操作改用标准 `rsa.SignPSS`/`SignPKCS1v15`；公钥加密改用 `rsa.EncryptOAEP`；删除手搓的大数函数。

### 34. `bcrypt` 未限制密码长度，超长密码（>72B）会被 bcrypt 静默截断
- 文件：[tpl/advanced/pkg/encrypt/bcrypt.go:13](tpl/advanced/pkg/encrypt/bcrypt.go#L13)
- 问题：bcrypt 密钥上限 72 字节，超长被截断，两个不同长前缀相同的密码会被视为相同。
- 建议：`BcryptHash` 前先 `if len(password) > 72 { return "", errors.New("password too long") }`，或先做一次 SHA-256 再 bcrypt。

### 35. `genDBRepo` 直连数据库用用户传入的 DSN，DSN 含明文密码，可能进日志
- 文件：[internal/create/genDBRepo.go:88-104](internal/create/genDBRepo.go#L88-L104)
- 问题：`gormDb.Migrator()` 失败时 `fmt.Errorf("connect db server fail: %w", err)`，gorm/mysql 错误信息可能回显 DSN 片段（含密码）。CLI 工具输出到终端，可能被终端录制/CI 日志采集。
- 建议：错误信息里脱敏 DSN（替换密码为 `***`）。

### 36. `availablePort` 存在竞态：拿到端口后到 `ListenAndServe` 之间端口可能被占
- 文件：[tpl/advanced/pkg/utils/net.go:18-26](tpl/advanced/pkg/utils/net.go#L18-L26)、[tpl/advanced/pkg/xserver/http/http.go:137-140](tpl/advanced/pkg/xserver/http/http.go#L137-L140)
- 问题：`AvailablePort` 监听后立即 Close，再交给业务 ListenAndServe，期间端口可能被抢，导致启动失败。
- 影响：低（默认配置 port=0 才触发）。
- 建议：文档提示生产环境固定端口。

---

## 三、并发与资源管理

### 37. `kafka.Subscriber.Close` 在 goroutine 里并发关闭 reader，但未等待关闭完成
- 文件：[tpl/advanced/pkg/kafka/subscriber.go:151-158](tpl/advanced/pkg/kafka/subscriber.go#L151-L158)
- 问题：`go func() { _ = reader.Close() }()`，Close 异步，调用方 `sub.Close()` 返回后 reader 可能还没关完，进程若立即退出可能丢未提交 offset。
- 建议：同步关闭，或用 WaitGroup 等待。

### 38. `ratelimit` 每次调用 `RateLimiter` 都起一个清理 goroutine，无退出机制
- 文件：[tpl/advanced/internal/middleware/ratelimit.go:34-63](tpl/advanced/internal/middleware/ratelimit.go#L34-L63)
- 问题：ticker goroutine 随实例回收，但若中间件频繁重建（如热重载路由）会累积 goroutine。注释已说明"随实例回收"，实际闭包变量 `records/banned` 无外部引用后 GC 可回收 goroutine（ticker 会被 GC 连带 Stop 吗？不会——time.NewTicker 不 Stop 会泄漏，但 GC 不回收活跃 ticker）。**这里 ticker 永远不被 Stop，goroutine 永远不退出**（除非进程退出）。这是内存/goroutine 泄漏。
- 建议：`RateLimiter` 返回 `(gin.HandlerFunc, func())` 提供 cleanup；或用 `context.Context` 控制 goroutine 生命周期，路由卸载时 cancel。

### 39. `asynq.go` 多处 `panic` 替代 error 返回
- 文件：[tpl/advanced/pkg/asynq/asynq.go:39-45](tpl/advanced/pkg/asynq/asynq.go#L39-L45)、[tpl/advanced/pkg/asynq/asynq.go:107-109](tpl/advanced/pkg/asynq/asynq.go#L107-L109)、[tpl/advanced/pkg/asynq/asynq.go:225-238](tpl/advanced/pkg/asynq/asynq.go#L225-L238)、[tpl/advanced/pkg/asynq/asynq.go:264-280](tpl/advanced/pkg/asynq/asynq.go#L264-L280)
- 问题：`New`、`CronStaticPub`、`CronPub`、`PeriodicTaskManager` 在配置错误时 `panic`。库函数 panic 让调用方无法优雅处理。`kafka.NewPublisher`/`NewSubscriber` 同样 panic（publisher.go:31, subscriber.go:32）。
- 建议：返回 error，由 wire/main 决定是否 panic。

### 40. `pool.go` 的 `Wait` 调用 `Shutdown` 又 `wg.Wait`，但 `AddTask` 在 Wait 后调用会 panic（向已关闭 channel 发送）
- 文件：[tpl/advanced/pkg/utils/pool.go:45-79](tpl/advanced/pkg/utils/pool.go#L45-L79)
- 问题：`Wait` 先 `close(taskQueue)`，若调用方在 Wait 后再 `AddTask` 会 panic。无防护。
- 建议：`AddTask` 用 `select { case p.taskQueue <- task: default: return errClosed }` 或维护 `closed` 标志。

### 41. `snowflake.GenID` 时钟回拨 panic 在消费协程里会直接崩进程
- 文件：[tpl/advanced/pkg/utils/snowflake.go:70-72](tpl/advanced/pkg/utils/snowflake.go#L70-L72)
- 问题：回拨 >5ms 直接 `panic`，若在业务 goroutine 调用会崩整个进程。`SonySnowFlake` 也 panic。
- 建议：返回 error，由调用方降级（用旧时间戳/等待）。

### 42. `xlog.KafkaWriter.Write` 每条日志都创建一个 3s 超时 context 并同步 WriteMessages
- 文件：[tpl/advanced/pkg/xlog/options.go:122-137](tpl/advanced/pkg/xlog/options.go#L122-L137)
- 问题：zap 的 `AddSync` 期望 Write 快速返回；这里同步写 kafka，kafka 抖动时会阻塞 zap 的写 goroutine，导致日志背压。且 `Async: true` 的 writer 仍可能在 broker 不可达时阻塞。
- 建议：日志写 kafka 用带缓冲的异步队列 + 丢日志策略，避免日志拖垮主业务。

### 43. `tracing.go` 的 `bufferPool` 在大 body chunked 分支里 `bufferPool.Put(buf)` 后，`requestBody` 引用的 `data` 是 `buf.Bytes()` 的 copy，OK；但 `responseWriter.body` 复用同一个 buf 池时，若 `c.Next()` 异步写响应（如 goroutine 里写），`defer bufferPool.Put(buf)` 在 handler 返回后执行，可能仍在写
- 文件：[tpl/advanced/internal/middleware/tracing.go:109-117](tpl/advanced/internal/middleware/tracing.go#L109-L117)
- 问题：`defer bufferPool.Put(buf)` 在中间件函数返回时执行（`c.Next()` 之后）。若业务 handler 起了 goroutine 在返回后继续 `ctx.Writer.Write`（错误用法但常见），会写到已归还池的 buf。属于误用防护缺失。
- 建议：可接受（gin handler 不应在返回后写响应），但建议注释强调。

---

## 四、Gin / HTTP 最佳实践

### 44. 全局 `gin.SetMode` 与 `gin.DefaultWriter` 在多个 `New` 中重复设置，且 `gin.New()` 后才 SetMode
- 文件：[tpl/advanced/pkg/xserver/http/http.go:57-65](tpl/advanced/pkg/xserver/http/http.go#L57-L65)
- 问题：`gin.New()` 先创建引擎，再 `gin.SetMode`。`gin.New()` 不读 mode，但 `gin.DefaultWriter = io.Discard` 是包级全局，多个 server 实例（http + broker health）会互相覆盖。
- 建议：在进程启动最早处统一 `gin.SetMode`，`New` 内不再设置全局状态。

### 45. 路由未使用 `routers []router.Router` 的分组能力，`v0.Demo` 直接 `e.Group`，缺少版本前缀中间件挂载点
- 文件：[tpl/advanced/internal/router/v0/demo.go:19-57](tpl/advanced/internal/router/v0/demo.go#L19-L57)
- 问题：`router.Router` 是 `func(e, jwt, hdl)`，版本 v0 靠包名区分，但没有 `/v0` 前缀，`global.RouterPrefixApi` 是 `/api`。版本演进时 v1/v2 如何挂载未约定。
- 建议：约定 router 签名带版本前缀，或 `rtrs` 按版本分组注册。

### 46. `demo` handler 大量内联匿名 struct 做 binding，未复用、未提取
- 文件：[tpl/advanced/internal/handler/demo/demo.go:48-88](tpl/advanced/internal/handler/demo/demo.go#L48-L88) 等
- 问题：每个 handler 内部 `req := &struct{...}{}`，参数对象无法复用、无法被 swagger 工具识别（需顶层命名类型）、测试不便。
- 建议：提取为命名 request 结构体（`DemoCreateReq` 等），便于校验、文档、复用。

### 47. `ShouldBind` 对 GET 请求 binding `form` tag，但 `Create` 用 `form+json` 双 tag，POST x-www-form-urlencoded 与 JSON 都能进——`ShouldBind` 行为依赖 Content-Type
- 文件：[tpl/advanced/internal/handler/demo/demo.go:114-125](tpl/advanced/internal/handler/demo/demo.go#L114-L125)
- 问题：gin `ShouldBind` 根据 Content-Type 选 binding：JSON 走 json binding，form 走 form binding。`Create` 同时写 `form` 和 `json` tag 是对的，但若客户端发 `Content-Type: application/json` 却传了 form 编码会 400。属于使用规范问题。
- 建议：可接受，文档说明。

### 48. `xhttp.BusFail` 对所有业务错误返回 `http.StatusOK`，RESTful 语义不规范
- 文件：[tpl/advanced/pkg/xhttp/response.go:81-97](tpl/advanced/pkg/xhttp/response.go#L81-L97)
- 问题：业务错误统一 200 + 业务 code。这是国内常见做法，但与 RESTful 冲突，监控/网关按 HTTP 状态码告警会漏掉业务错误。
- 建议：团队约定即可；若要 RESTful，按错误类型映射 HTTP 状态。

### 49. `Recovery` 中间件非 broken-pipe 时 `AbortWithStatus(500)` 但未写响应体
- 文件：[tpl/advanced/internal/middleware/recovery.go:76](tpl/advanced/internal/middleware/recovery.go#L76)
- 问题：`ctx.AbortWithStatus(http.StatusInternalServerError)` 只设状态码，无 body。客户端收到空 500。建议返回统一 JSON 错误。
- 建议：`ctx.JSON(500, xhttp.Response{Code: 500, Message: "Internal Server Error"})`，但注意此时可能 header 已写（tracing 包装的 writer），需在 `c.Next()` 前 abort。

### 50. `tracing.go` 对 POST/PUT body 全量读取并重建，multipart 32MB 限制硬编码
- 文件：[tpl/advanced/internal/middleware/tracing.go:70-71](tpl/advanced/internal/middleware/tracing.go#L70-L71)
- 问题：`ParseMultipartForm(32 << 20)` 硬编码 32MB，且把整个 form 序列化进日志（虽有 `FilterForm` 脱敏），大表单会占内存+日志体积。
- 建议：multipart 仅记录字段名与大小，不记录值。

### 51. `router.Ping` 路由 `/ping` 未做鉴权/限流，可被刷
- 文件：[tpl/advanced/internal/router/router.go:27-31](tpl/advanced/internal/router/router.go#L27-L31)
- 问题：健康检查路由公开，生产可被外部刷。若 k8s liveness probe 内部访问无妨，但暴露公网有风险。
- 建议：ping 返回最小响应，或限制来源。

---

## 五、GORM / 数据访问

### 52. `defaultDemoDb.Insert` 清零 `data.Id`，但非自增主键场景会丢失调用方传入的 Id
- 文件：[tpl/advanced/internal/repository/db/demo_gen.go:74-82](tpl/advanced/internal/repository/db/demo_gen.go#L74-L82)
- 问题：注释说"自增主键清零，非自增主键保留调用方传入值"，但代码无条件 `data.Id = zero`，非自增主键也会被清零，与注释矛盾。
- 建议：根据 `PrimaryKeyAutoIncrement` 决定是否清零；或至少注释与实现一致（当前实现是"总是清零"）。

### 53. `FindByIds` 未校验空 `ids`，`WHERE id IN ()` 在部分驱动下是语法错误
- 文件：[tpl/advanced/internal/repository/db/demo_gen.go:123-130](tpl/advanced/internal/repository/db/demo_gen.go#L123-L130)
- 问题：`ids` 为空切片时，gorm 生成的 `IN ()` 在 MySQL 报错。
- 建议：`if len(ids) == 0 { return nil, nil }`。`Delete`/`SoftDelete` 同理。

### 54. `HandleRank` 白名单 `fields` 用逗号分隔字符串匹配，含表前缀时 `defaultCol[:i+1] + name` 可能拼出错误表名
- 文件：[tpl/advanced/internal/repository/db/mysql.go:82-95](tpl/advanced/internal/repository/db/mysql.go#L82-L95)
- 问题：`defaultCol = "demo.id"`，`name="name"` 命中白名单后 `col = "demo." + "name" = "demo.name"`，正确。但若 `defaultCol` 不含 `.`（如 `"id"`），走 `col = name`，深分页 JOIN 时列名歧义。逻辑虽在注释说明，但依赖 `defaultCol` 一定带表前缀这一隐式约定。
- 建议：强制 `defaultCol` 带 `table.` 前缀，函数内断言。

### 55. `ListWithTotal` 在 `total == 0` 时返回 `ErrNotFound`，但 `FindList` 调用方把 `ErrNotFound` 当"空列表"处理
- 文件：[tpl/advanced/internal/repository/db/demo.go:58-60](tpl/advanced/internal/repository/db/demo.go#L58-L60)、[tpl/advanced/internal/service/svc/demo.go:149-154](tpl/advanced/internal/service/svc/demo.go#L149-L154)
- 问题：`total==0` 返回 `ErrNotFound`，service 层 `errors.Is(listErr, db.ErrNotFound)` 返回空列表。语义混淆：找不到记录 vs 真正的查询错误。且 `ListWithMore` 在 `ln==0` 也返回 `ErrNotFound`，但 `total>0` 而 `Find` 查不到的情况被吞成空。
- 建议：空结果返回 `(nil, 0, nil)` 而非 `ErrNotFound`，`ErrNotFound` 仅用于单条 `Find` 找不到。

### 56. `FindStream` 用 `db.Rows()` 后 `db.ScanRows`，但 `db` 是 `c.WithContext(ctx).Model(c.model).Order(...)` 的派生 DB
- 文件：[tpl/advanced/internal/repository/db/demo.go:168-187](tpl/advanced/internal/repository/db/demo.go#L168-L187)
- 问题：`rows, err := db.Rows()` 后 `db.ScanRows(rows, &item)`——`db` 同一变量，gorm 文档建议用 `db.ScanRows`。正确。但 `rows.Close()` 在 `handler` 返回 error 时由 defer 执行，OK。无 bug，仅确认。

### 57. `xdb` 未配置 `PrepareStmt`，高频查询建议开启预编译
- 文件：[tpl/advanced/pkg/xdb/xdb.go:55-59](tpl/advanced/pkg/xdb/xdb.go#L55-L59)
- 问题：`gorm.Config` 未设 `PrepareStmt: true`，高频查询每次都解析 SQL。
- 建议：按场景开启 `PrepareStmt: true`（注意与 `dbresolver` 兼容性）。

### 58. `gorm.Config` 的 `SkipInitializeWithVersion: true` 导致不按 MySQL 版本自动适配特性
- 文件：[tpl/advanced/pkg/xdb/xdb.go:51](tpl/advanced/pkg/xdb/xdb.go#L51)
- 问题：`SkipInitializeWithVersion: true` 跳过版本探测，注释说"根据当前 MySQL 版本自动配置"——但实际 Skip 是"跳过"，与注释相反。
- 建议：核对注释，明确是否需要版本自适应。

---

## 六、Go 语言用法 / 可维护性

### 59. `intToBase` 用 `[]byte` append 后反转，可用 `strings.Builder` 或预分配
- 文件：[tpl/advanced/pkg/utils/convert.go:22-36](tpl/advanced/pkg/utils/convert.go#L22-L36)
- 建议：小优化，预分配 `make([]byte, 0, 64)`。

### 60. 大量 `fmt.Errorf("%w", err)` 包裹但未保留栈
- 文件：多处，如 [internal/new/new.go](internal/new/new.go)、[tpl/advanced/pkg/token/jwt.go](tpl/advanced/pkg/token/jwt.go)
- 问题：用 `fmt.Errorf` 包裹错误无栈追踪，定位困难。项目已引入 `github.com/pkg/errors`（svc/demo.go 用），但部分包混用 `fmt.Errorf`。
- 建议：统一错误包裹方式，关键路径用 `errors.Wrap`/`errors.WithStack`。

### 61. `xerror.NewError` 在构造时即 `Log(ctx)`，service 层每处错误都打日志，handler 层 `BusFail` 不再打——但 `defaultError.Error()` 返回 message 含错误码，重复日志风险
- 文件：[tpl/advanced/pkg/xerror/error.go:43-54](tpl/advanced/pkg/xerror/error.go#L43-L54)
- 问题：`NewError` 内部 `e.Log(ctx)` 立即记录，调用方返回 error 后 handler 又可能记录。`xlog.Errorf` 里再 `errCode(e.err)` 一次。
- 建议：明确"在 service 层 NewError 即记录，handler 不再重复记录"的约定，避免重复日志。

### 62. `cmd/kun/root.go` 的 `CmdCreate` RunE 是空实现 `return nil`，参数校验由子命令负责
- 文件：[internal/create/create.go:28-34](internal/create/create.go#L28-L34)
- 问题：`kun create` 不带子命令时静默成功（exit 0），用户无反馈。
- 建议：`RunE` 改为返回 `fmt.Errorf("create requires a subcommand (hdl/svc/hs/rt/db/cache)")`，或用 `cobra.NoArgs` + 提示。

### 63. `internal/wire/wire.go` 的 `findWire` 向上搜索时 `base = filepath.Join(base, "..")`，但 `walkDir(base)` 每次全量遍历 base 目录
- 文件：[internal/wire/wire.go:156-169](internal/wire/wire.go#L156-L169)
- 问题：每层都 `filepath.Walk(base)` 全量遍历，向上 5 层 × 全量 walk，大仓库慢。
- 建议：向上找 `go.mod` 时只 `os.Stat`，找到项目根后一次性 walk。

### 64. `helper.FindMain` 读所有 cmd 下 .go 文件全文匹配 `func main(`
- 文件：[pkg/helper/helper.go:82-96](pkg/helper/helper.go#L82-L96)
- 问题：用正则匹配 `func main(`，但 `func main  (`（多空格）也匹配，`func mainx(` 不匹配——OK。但读整个文件较慢，可用 `go/parser` 或只读前 N 行（package main 通常在开头，但 func main 可能在末尾）。
- 建议：可接受，小优化。

### 65. `output` 包用全局 `color.NoColor`，非 TTY 检测只看 stdout
- 文件：[pkg/output/output.go:10-19](pkg/output/output.go#L10-L19)
- 问题：`Error` 写到 stdout（`color.Red` 走 stdout），若 stderr 重定向而 stdout 是 TTY，颜色码可能混进管道。一般 CLI 错误应走 stderr。
- 建议：`Error` 用 `color.New(...).Fprint(os.Stderr, ...)`，错误信息走 stderr。

### 66. `new.go` 的 `replaceFiles` 全文替换 import path，但替换后未 `gofmt`/`goimports`
- 文件：[internal/new/new.go:191-221](internal/new/new.go#L191-L221)
- 问题：`bytes.ReplaceAll` 替换 `"advanced/` → `"myproj/`，但若模板里 import 注释或字符串含 `"advanced/` 也会被替换（注释已说只替换 import 前缀，但 `"` + `advanced/` 的组合在字符串里也可能出现）。
- 建议：替换后跑一次 `goimports -w`（已依赖 go 工具链）。

### 67. `genDBRepo` 未关闭 gorm 连接
- 文件：[internal/create/genDBRepo.go:101-133](internal/create/genDBRepo.go#L101-L133)
- 问题：`connectDB` 返回的 `*gorm.DB` 在生成完成后未 `sqlDB.Close()`，CLI 进程退出会释放，但显式关闭更佳。
- 建议：`defer sqlDB.Close()`。

### 68. `sqlParser.go` 的 `findMatchingParen` / `splitByComma` 用 byte 遍历，对多字节 SQL（中文注释/字符串）的引号判断可能错位
- 文件：[internal/create/kernel/sqlParser.go:86-121](internal/create/kernel/sqlParser.go#L86-L121)
- 问题：按 byte 处理 `\'` 转义和 `''` 转义，但 SQL 字符串里中文是 UTF-8 多字节，byte 遍历不会误判引号（引号是单字节 ASCII），实际安全。仅当 SQL 用 GBK 编码且中文含 `0x27` 字节时才有问题——`convertToUTF8` 已转 UTF-8。
- 建议：可接受。

### 69. `sqlParser.go` `convertToUTF8` 无 BOM 的 UTF-16 LE 启发式检测可能误判
- 文件：[internal/create/kernel/sqlParser.go:572-579](internal/create/kernel/sqlParser.go#L572-L579)
- 问题：启发式 `data[0]!=0 && data[1]==0 && data[2]!=0 && data[3]==0` 对纯 ASCII 的 UTF-8 文件（每两字节第二字节为 0）会误判为 UTF-16 LE 并解码成乱码。
- 后果：普通 ASCII SQL 文件被错误解码。
- 建议：仅在带 BOM 时转换；无 BOM 的 UTF-16 检测不可靠，应要求用户提供 UTF-8/带 BOM 文件，或用 `golang.org/x/text/encoding/unicode` 的更稳健检测。

### 70. `convertToUTF8` 的 `decodeUTF16LE` 不处理代理对（surrogate pair）
- 文件：[internal/create/kernel/sqlParser.go:583-592](internal/create/kernel/sqlParser.go#L583-L592)
- 问题：`rune(data[i])|rune(data[i+1])<<8` 直接拼，遇到 emoji 等需代理对的字符会得到孤立代理 rune，`string(runes)` 会产生替换字符。
- 影响：低（SQL 文件少有 emoji）。
- 建议：用 `unicode/utf16.Decode`。

---

## 七、配置 / 启动

### 71. `xconfig.New` 在 `ReadInConfig`/`Unmarshal` 失败时 `panic`
- 文件：[tpl/advanced/pkg/xconfig/xconfig.go:115-121](tpl/advanced/pkg/xconfig/xconfig.go#L115-L121)
- 问题：配置错误 panic，main 里 `panic(err)` 会打印栈。可接受（fail fast），但 `WatchConfig` 的 `OnConfigChange` 用 `fmt.Printf` 而非 xlog。
- 建议：热重载失败走 xlog。

### 72. `xconfig` 环境变量覆盖在 `Unmarshal` 之后才 `AutomaticEnv`，首次读取拿不到环境变量覆盖
- 文件：[tpl/advanced/pkg/xconfig/xconfig.go:118-127](tpl/advanced/pkg/xconfig/xconfig.go#L118-L127)
- 问题：`v.Unmarshal(c)` 在 `AutomaticEnv()` 之前，环境变量覆盖对首次 unmarshal 无效。需二次 `v.Unmarshal` 或把 `AutomaticEnv` 提前。
- 后果：`SERVER_PORT=10088` 不会在首次启动生效（除非配置文件里没有该 key 时 viper 回退 env——但 Unmarshal 已发生）。
- 建议：`AutomaticEnv()` / `SetEnvKeyReplacer` 放在 `ReadInConfig` 之前。

### 73. `main.go` 的 `flag.String("conf", "config/local.yml", ...)` 与 `xconfig.New` 里 `os.Getenv("CONF")` 优先级重复
- 文件：[tpl/advanced/cmd/server/main.go:21](tpl/advanced/cmd/server/main.go#L21)、[tpl/advanced/pkg/xconfig/xconfig.go:106-109](tpl/advanced/pkg/xconfig/xconfig.go#L106-L109)
- 问题：main 用 flag 接收 `-conf`，传给 `New`；`New` 又优先读 `CONF` 环境变量。两套配置入口，`-conf` 会被 `CONF` 覆盖，易混淆。
- 建议：统一一种方式，或文档说明优先级。

### 74. `AwaitSignal` 用 `os.Exit(0)`，跳过 main 的 defer；已注释说明但仍有风险
- 文件：[tpl/advanced/pkg/xserver/xserver.go:42-49](tpl/advanced/pkg/xserver/xserver.go#L42-L49)
- 问题：`os.Exit(0)` 跳过 defer，所有清理必须在 `eng.Stop` 内。当前 http/broker 的 Stop 已含完整清理，但未来新增资源若用 defer 管理会泄漏。
- 建议：改为 `Stop` 后 `return`，让 main 自然退出；或维持现状但加 lint 规则禁止 main defer 资源。

---

## 八、其它优化点

### 75. `tpl/advanced/internal/global/pay.go` / `event.go` 等常量定义建议确认（未读，建议复核）
- 建议：检查 `EventTopic`/`EventType` 常量与 `asynq`/`kafka` topic 是否一致，避免拼写错误导致消息发到不存在 topic。

### 76. `cmd/kun/root.go` 用 `var CmdRoot = &cobra.Command{...}` 包级变量，测试不便
- 建议：大型 CLI 可考虑构造函数，便于注入 mock。当前规模可接受。

### 77. 大量 `// TODO` 与注释掉的代码块（如 `subscriber.go` 末尾、`publisher.go` 末尾、`new.go` 的 `handlerFiles`）
- 文件：[internal/new/new.go:297-339](internal/new/new.go#L297-L339)、[tpl/advanced/internal/event/subscriber.go:141-229](tpl/advanced/internal/event/subscriber.go#L141-L229)
- 建议：删除注释代码，避免噪声；TODO 进 issue tracker。

### 78. `demoSvc.SendMsg` 单个方法里演示 6 种消息发送，生产代码不应如此聚合
- 文件：[tpl/advanced/internal/service/svc/demo.go:351-449](tpl/advanced/internal/service/svc/demo.go#L351-L449)
- 建议：拆分为独立示例 handler，或注释说明仅 demo。

### 79. `xerror.errCode` 用 MD5 前 20bit 生成 4 位 base36 错误码，碰撞概率
- 文件：[tpl/advanced/pkg/xerror/error.go:75-86](tpl/advanced/pkg/xerror/error.go#L75-L86)
- 问题：20bit = 约 100 万空间，base36 4 位 = 36^4 ≈ 167 万，碰撞概率随错误数增长。错误码用于日志检索，碰撞会误导。
- 建议：可接受（错误码仅辅助），或扩大到 6 位。

### 80. `xlog` 的 `logContent` 把所有参数塞进 `Params any`（取 `params[0]`），多参数丢失
- 文件：[tpl/advanced/pkg/xlog/xlog.go:154-163](tpl/advanced/pkg/xlog/xlog.go#L154-L163)
- 问题：`Info(ctx, message, params...)` 只取 `params[0]`，`Info(ctx, "x", a, b)` 的 `b` 丢失。
- 建议：`Params: params`（整个切片）或强制单参数。

### 81. `tracing.go` 记录响应 body 到 span attribute，可能泄漏敏感数据
- 文件：[tpl/advanced/internal/middleware/tracing.go:166-178](tpl/advanced/internal/middleware/tracing.go#L166-L178)
- 问题：`http.response_body` 记录前 512 字节响应体，若响应含 token/密码等敏感字段（虽有 `FilterContent`，但 span attribute 用的是 `responseInfo.Body` 已过滤），需确认 FilterContent 对 JSON 响应生效。
- 建议：确认 `w.Header().Get("Content-Type")` 传给 FilterContent 正确；敏感接口可不记录 response body。

### 82. `otel.InitTracer` 采样率固定 10%
- 文件：[tpl/advanced/internal/middleware/otel.go:63](tpl/advanced/internal/middleware/otel.go#L63)
- 问题：`TraceIDRatioBased(0.1)` 硬编码，不同环境应可配。
- 建议：从配置读采样率。

### 83. `broker` 与 `http` server 的 `Stop` 都 `fmt.Printf` 打 logger sync 错误
- 文件：[tpl/advanced/pkg/xserver/http/http.go:214-216](tpl/advanced/pkg/xserver/http/http.go#L214-L216)
- 问题：`fmt.Printf("Failed to sync logger: %v\n", err)`，此时 xlog 可能已关闭，用 fmt 合理，但建议统一用 stderr。
- 建议：`fmt.Fprintf(os.Stderr, ...)`。

### 84. `swagger/docs.go` 为生成文件，但 `SwaggerInfo.BasePath = ""` 在 router 里设置
- 文件：[tpl/advanced/internal/router/router.go:35-36](tpl/advanced/internal/router/router.go#L35-L36)
- 建议：确认 swagger 生成命令配置正确，BasePath 与路由前缀一致。

### 85. `go.mod` 未审查（建议补充 `go 1.20+` 以支持 `http.ResponseController`）
- 文件：[go.mod](go.mod)
- 建议：确认 `go` directive ≥ 1.20，否则 demo.go 的 SSE/Export 流式接口会编译失败或运行异常。

---

## 九、优先级建议

| 优先级 | 编号 | 概要 |
|--------|------|------|
| P0（数据正确性/安全） | 1, 24, 6, 33, 34, 29, 69 | AES 流模式填充、缓存对象不完整致返回零值、wire 去重误判、RSA 手搓大数、bcrypt 截断、JWT 弱密钥、UTF-16 误判 |
| P1（功能/可靠性） | 2, 5, 7, 9, 14, 52, 53, 55, 72 | intToBase 溢出、jwt 绑定字段、create import 注入、Slowloris、broker 关闭时序、Insert 清零、空 ids IN()、空结果语义、env 覆盖时序 |
| P2（并发/资源） | 38, 39, 40, 41, 37, 42 | ratelimit goroutine 泄漏、库 panic、pool 关闭后 AddTask panic、snowflake panic、kafka Close 异步、日志 kafka 阻塞 |
| P3（规范/可维护） | 4, 10, 19, 28, 30, 44, 46, 48, 60, 66, 71, 77, 80, 82 | 金额 float64、热重载文档、log API 不对称、IP 伪造、401→201、gin 全局状态、匿名 struct、RESTful、错误包裹、replace 后 goimports、配置 panic、注释代码、日志多参数、采样率配置 |

---

## 十、总结

项目整体架构清晰（wire 分层、模板生成、多进程 broker、全链路追踪），代码注释详尽，已修复大量常见陷阱（zip slip、SQL 注入白名单、jwt 算法降级、asynq client 泄漏等）。主要问题集中在：

1. **加密库**：AES 流模式误填充、RSA 手搓大数运算——建议优先用标准库安全 API。
2. **模板示范**：缓存对象字段不完整、金额用 float64、事务示范删相邻 id——会被新手复制成生产 bug。
3. **生成代码注入**：wire2DI 的去重与 import 注入逻辑脆弱，多次生成可能漏注册。
4. **并发资源**：ratelimit/kafka 的 goroutine 生命周期管理、库函数 panic 习惯。
5. **HTTP 规范**：401 返 201、空 500 响应、Slowloris 防护缺失、长连接与全局 WriteTimeout 冲突。

建议按优先级表逐项修复，P0 项影响数据正确性与安全，应尽快处理。
