# 优化报告：tpl/basic & tpl/advanced

> 审查范围：`tpl/basic` 与 `tpl/advanced` 两个模板的全部 Go 源码。
> 审查目标：检查 Bug、提出优化方案，推动**高内聚、低耦合**。
> 结论：两个模板整体架构清晰（handler → service → repository 分层 + wire DI），安全细节（JWT 黑名单故障关闭、CSRF、限流、雪花时钟回拨、kafka 毒消息跳过）考虑周到。但仍存在 **2 个真实 Bug**、若干 **并发/资源隐患**、以及大量 **两模板代码漂移与重复** 问题。下面按优先级列出。

---

## 一、真实 Bug（应优先修复）

### 1. [BUG] `utils.ClientIP` 注释与实现矛盾，且实现本身可被伪造
- **文件**：[basic/pkg/utils/net.go:90-107](tpl/basic/pkg/utils/net.go#L90-L107) / [advanced/pkg/utils/net.go:90-107](tpl/advanced/pkg/utils/net.go#L90-L107)
- **问题**：
  1. 函数注释写着「直接使用 gin 的 `c.ClientIP()`，避免直接信任可伪造的头」，但实现却是**先手动读 `X-Real-IP` / `X-Forwarded-For`**，最后才用 `c.ClientIP()`。手动读头正是注释所警告的可伪造行为，注释与代码完全相反。
  2. 逻辑层层覆盖（X-Real-IP → X-Forwarded-For → 127.0.0.1 → RemoteIP → ClientIP），只要任一头存在就会信任，绕过了 gin 的 `SetTrustedProxies` 可信代理校验，攻击者可伪造任意 IP 绕过基于 IP 的限流（见 `ratelimit.go` 用 `ctx.ClientIP()`）。
- **方案**：删除手动解析头的逻辑，直接 `return c.ClientIP()`，并在文档中说明反向代理场景下必须正确配置 `SetTrustedProxies`。若确需保留多源回退，应仅从可信代理白名单内取值。

### 2. [BUG] basic 模板 `DemoListArgs.OrderType` 注释语义反了
- **文件**：[basic/internal/service/svc/demo.go:41](tpl/basic/internal/service/svc/demo.go#L41)
- **问题**：basic 注释为 `OrderType int64 // 排序类型 0:升序,1:降序`，但 `HandleRank`（[mysql.go:102-105](tpl/basic/internal/repository/db/mysql.go#L102-L105)）实际是 `orderType==1 → ASC，否则 DESC`，即 **0:降序,1:升序**。advanced 模板同位置注释正确。该错误注释会误导使用者传反排序方向。
- **方案**：将 basic 注释改为 `0:降序(默认),1:升序`，与 advanced 及 `SearchPage` 注释统一。

---

## 二、并发与资源隐患

### 3. [并发] `kafka.Publisher` 的 `Close()` 与 `getWriter()` 存在 data race
- **文件**：[advanced/pkg/kafka/publisher.go:40-60](tpl/advanced/pkg/kafka/publisher.go#L40-L60)
- **问题**：`getWriter()` 用 `sync.Once` 懒初始化 `p.writer`，但 `Close()` 直接无锁读 `p.writer != nil` 再调用 `p.writer.Close()`。若 Close 与一次并发 Publish 的 Once 初始化交错，`Close` 可能读到 nil（漏关）或在 Once 赋值的瞬间读到半初始化值（race）。`go vet`/`-race` 下会告警。
- **方案**：Close 也走 `p.once.Do` 或用 `atomic.Pointer[Writer]`；或在 `NewPublisher` 时立即创建 Writer（去掉懒加载），Close 时直接判空关闭。推荐后者：构造期建好，生命周期清晰，且消除 race。

### 4. [全局态] `xlog.kafkaCloser` 是包级可变全局，多实例互相覆盖
- **文件**：[advanced/pkg/xlog/xlog.go:31,104,123-128](tpl/advanced/pkg/xlog/xlog.go#L31)
- **问题**：`kafkaCloser` 是包级 `var`。`New()` 每次调用都覆盖它，`Close()` 只关最后一个。broker 模板中父进程与 fork 出的子进程各自 `New` 一个 logger，后建的会覆盖前者的 closer，导致先建的 Kafka Writer 永不关闭（缓冲日志丢失/连接泄漏）。`Close()` 也非并发安全。
- **方案**：把 closer 作为 `*Logger` 的一部分（或返回一个带 `Close()` 的包装类型），由持有 logger 的 `Server` 在 `Stop` 中关闭自己那份，移除包级全局。`http.Server` 与 `broker.Server` 的 `Stop()` 已经各自调 `xlog.Close()`，正好可改为关闭实例级 closer。

### 5. [健壮性] `http.Server` 端口监听失败后 `Stop("")` 触发资源二次关闭
- **文件**：[advanced/pkg/xserver/http/http.go:162-167](tpl/advanced/pkg/xserver/http/http.go#L162-L167) 与 [basic 同名文件](tpl/basic/pkg/xserver/http/http.go)
- **问题**：goroutine 中 `ListenAndServe` 返回非 `ErrServerClosed` 错误时调用 `s.Stop("")`，它会关 http/db/redis/kafka/tracer/xlog。随后 `AwaitSignal` 收到信号又会再次调用 `s.eng.Stop(...)`，对已关闭的 db/redis 重复 Close（多数驱动可幂等，但 tracer/xlog 的二次关闭会打 stderr 噪音，kafka publisher 二次 Close 可能 error）。
- **方案**：监听失败分支改为只 `s.server.Shutdown(ctx)` + 日志 + 让进程退出（`os.Exit(1)` 或通过 channel 通知主流程），不要复用完整的 `Stop()`。或用 `sync.Once` 守卫 `Stop`，确保整套关闭只执行一次。

### 6. [资源] `ratelimit.go` 清理协程随实例泄漏风险已规避，但封禁判定有 TOCTOU
- **文件**：[advanced/internal/middleware/ratelimit.go:68-102](tpl/advanced/internal/middleware/ratelimit.go#L68-L102)
- **问题**：`records[ip]` 与 `banned[ip]` 在同一把 `mu` 下，逻辑正确；但 `ctx.ClientIP()` 在持锁前调用没问题。唯一可改进点：超限封禁后 `delete(records, ip)` 再 `banned[ip]=...`，窗口内已累积的计数被丢弃——封禁期内无影响，但解封后从 0 重新计，属于可接受语义。此条仅作记录，非必须改。
- **方案**：保持现状；若要严格滑窗，可保留 records。

---

## 三、架构与耦合（高内聚低耦合）

### 7. [耦合] `pkg/xserver/http` 直接 import `internal/{handler,middleware,router,event}`——pkg 反向依赖 internal
- **文件**：[advanced/pkg/xserver/http/http.go:18-21](tpl/advanced/pkg/xserver/http/http.go#L18-L21)
- **问题**：`pkg/` 本应是可被任意上层复用的基础库，但 `xserver/http` 反向 import 了 `internal/handler`、`internal/middleware`、`internal/router`、`internal/event`。这导致该 pkg 完全无法独立复用，也使「基础库 ↔ 业务层」形成环向耦合。`broker.go` 同样 import 了 `internal/{event,middleware,router}`。
- **方案**：反转依赖。让 `internal/app`（或 `cmd/server`）负责装配：`xserver/http` 只暴露 `New(engine *gin.Engine, conf, logger, ...) Engine`，把「注册中间件、挂路由、装 handler」的编排逻辑上移到 `internal`。即依赖方向应为 `internal → pkg`，而非 `pkg → internal`。这是当前最值得做的解耦改造。

### 8. [内聚] gin 全局态（`SetMode`/`DefaultWriter`）在 3 处重复设置
- **文件**：[http.go:58-64](tpl/advanced/pkg/xserver/http/http.go#L58-L64)、[broker.go:236-242](tpl/advanced/pkg/xserver/broker/broker.go#L236-L242)、healthServer 内
- **问题**：设置 gin 模式与 `DefaultWriter` 的逻辑在 http server、broker health server 各抄一遍，注释也重复。gin 全局态本身是隐患（多实例互相覆盖），分散设置更易漏。
- **方案**：抽到 `pkg/xserver` 或 `pkg/utils` 一个 `InitGinMode(env string)` 函数，启动期调用一次；或彻底放弃改全局，改用 `gin.NewWithConfig` 实例级配置。

### 9. [重复] `Stop()` 关闭序列在 http / broker 两份高度雷同
- **文件**：[http.go:173-231](tpl/advanced/pkg/xserver/http/http.go#L173-L231) vs [broker.go:157-228](tpl/advanced/pkg/xserver/broker/broker.go#L157-L228)
- **问题**：两者都做「关 health/http server → 关 db → 关 redis → 关 kafka → 关 tracer → sync logger → xlog.Close」，结构几乎一致，字段名/日志措辞略有不同。新增资源（如 es/mongo）要改两处。
- **方案**：抽 `pkg/xserver.Lifecycle`（或 `Closer` 聚合器），按 LIFO 顺序注册关闭函数，`Stop` 统一遍历调用。http 与 broker 只注册各自额外的资源。

### 10. [重复] `DemoListArgs` / `Demo` / 搜索过滤逻辑在 basic 与 advanced 各存一份且已漂移
- **文件**：[basic/internal/service/svc/demo.go](tpl/basic/internal/service/svc/demo.go) vs [advanced/internal/service/svc/demo.go](tpl/advanced/internal/service/svc/demo.go)
- **问题**：advanced 是 basic 的超集（多 Login/WS/SSE/Excel/事件），但两边 CRUD 主体几乎相同，注释与字段已开始漂移（见 Bug #2 的 OrderType 注释；basic `Demo` 无 `RoleId`/`Password` 字段）。这种「复制-改」的维护方式注定持续漂移。
- **方案**：模板生成器（`kun`）层面应让 advanced 模板的 basic 子集从同一份源生成，或在 advanced 中直接包含 basic 的全部能力、basic 仅作为「裁剪掉 broker/event 的子集」由生成器产出。短期内至少保证两边注释与字段语义手工对齐。

---

## 四、两模板代码漂移（应统一）

### 11. [漂移] basic 用 CRLF、advanced 用 LF，造成大量伪 diff
- **文件**：`pkg/utils/net.go`、`pkg/utils/decimal.go` 等几乎所有「DIFFERS」文件
- **问题**：`diff` 显示 260 行差异的 `net.go`，去除 `\r` 后内容完全相同。两模板行尾不一致，严重干扰 diff/code review，也易让生成器误判需更新。
- **方案**：统一为 LF（`.gitattributes` 已在 advanced 有，basic 缺）。建议在仓库根加 `.gitattributes`：`*.go text eol=lf`，并 `git add --renormalize .` 一次性归一。advanced 已有 `.gitattributes`，可复用其规则。

### 12. [漂移] `pkg/` 下 19 个「真实差异」文件多为 import 路径替换 + 少量增量
- **文件**：见下表
- **问题**：绝大多数差异只是 `basic/pkg/...` → `advanced/pkg/...` 的模块名替换；真正有语义差异的只有：
  - `snowflake.go`：advanced 多了 `InitSonyFlake`/`SonySnowFlake`（basic 缺这套 API）
  - `xconfig.go`：advanced 多 `Broker`/`Kafka` 配置段
  - `xerror/code.go`：advanced 多 `AccountOrPasswordError`(2006)
  - `xserver/http/http.go`：advanced 多 `event.Pub` 注入、Swagger 路由、kafka publisher 关闭
  - `base36.go`：advanced 多 10 个 0（错误码空间扩展）
- **方案**：这些差异本身合理（超集），但应通过生成器从单一源 + 增量 patch 产出，而非手工维护两份。短期确保 advanced 的增量逻辑不被反向同步覆盖 basic。

### 13. [死代码] `ErrSonyflakeNotInit` 声明未使用
- **文件**：[advanced/pkg/utils/snowflake.go:29](tpl/advanced/pkg/utils/snowflake.go#L29) 与 :166
- **问题**：声明了 `ErrSonyflakeNotInit`，但 `SonySnowFlake()` 第 166 行直接 `panic("sonyflake not initialized...")` 用裸字符串，未用该 error。`InitSonyFlake` 失败也是 `panic(sonyInitErr)`。
- **方案**：要么 `panic(ErrSonyflakeNotInit)`，要么改为返回 error（与 `Snowflake.GenID` 返回 error 的降级风格一致，避免在业务协程里 panic 崩进程）。推荐后者。

---

## 五、小问题与风格

### 14. [一致性] `xerror.NewError` 当 `err==nil` 时不打日志也不附错误码
- **文件**：[advanced/pkg/xerror/error.go:43-54](tpl/advanced/pkg/xerror/error.go#L43-L54)
- **问题**：`NewError(ctx, code, msg, nil)` 时 `e.err==nil`，既不 `Log` 也不拼「错误代码」。调用方多处传 `nil`（如 `xerror.NewError(ctx, xerror.InvalidArgument, "invalid id", nil)`），这些错误不会有错误码用于排障。
- **方案**：业务约定明确：纯参数校验类错误传 nil 可接受；但若希望可追踪，应统一为始终生成错误码（基于 message 哈希）。

### 15. [可读性] `demoSvc.SendMsg` 单方法内发 6 种消息，耦合演示逻辑
- **文件**：[advanced/internal/service/svc/demo.go:356-454](tpl/advanced/internal/service/svc/demo.go#L356-L454)
- **问题**：一个方法里硬编码 6 个 `PayRecharge` 示例分别走 Kafka/KafkaWithType/Delay/Sync/Cron，重复的 marshal+error 包装 6 遍。
- **方案**：作为 demo 可接受；若要清爽，抽 `publishOne(pub func() error, label string) error` 统一 marshal+wrap。优先级低。

### 16. [文档] `publisher.go` 大段注释掉的 `PayRecharge*` 代码
- **文件**：[advanced/internal/event/publisher.go:126-202](tpl/advanced/internal/event/publisher.go#L126-L202)
- **问题**：~75 行注释代码长期保留，增加噪声。
- **方案**：删除，或移到单独的 `_examples` 文件/README。

### 17. [健壮性] `xconfig.Conf.Get()` 对热重载后的快照再调用会返回 nil
- **文件**：[advanced/pkg/xconfig/xconfig.go:133-152](tpl/advanced/pkg/xconfig/xconfig.go#L133-L152)
- **问题**：热重载时 `nc := new(Conf); v.Unmarshal(nc); c.current.Store(nc)`。新快照 `nc` 的 `current` 字段是零值 `atomic.Pointer`（未 `Store`），如果有人对 `Get()` 返回的快照再次调 `.Get()`，会拿到 nil。当前代码似乎只在根 `conf` 上调 `Get()`，未触发，但 API 有 footgun。
- **方案**：让快照不可二次 `Get()`——要么 `Get()` 返回一个不含 `current` 的只读视图类型，要么在文档/类型上禁止。或在 `OnConfigChange` 中 `nc.current.Store(nc)` 自指（与初始一致），使任意快照都能继续 `Get()`。推荐自指方案，最小改动。

---

## 六、优化优先级建议

| 优先级 | 项 | 收益 |
|---|---|---|
| P0 | #1 ClientIP 伪造风险 | 安全：限流可被绕过 |
| P0 | #3 kafka Publisher race | 并发正确性 |
| P1 | #7 pkg→internal 反向依赖 | 架构解耦，核心 |
| P1 | #4 xlog 全局 closer | 资源泄漏（broker 父子进程） |
| P1 | #5 http Stop 二次关闭 | 优雅关闭健壮性 |
| P2 | #2 OrderType 注释 | 文档正确性 |
| P2 | #11 行尾统一 | 维护性 |
| P2 | #9 Stop 序列抽取 | 去重 |
| P3 | #8 gin 全局态抽取、#10 demo 去重、#13 死错误、#17 Get footgun | 内聚/可维护 |

---

## 附：值得肯定的设计（勿动）

- **JWT**：黑名单查询失败时「故障关闭」而非放行（[jwt.go:276-281,320-327](tpl/advanced/pkg/token/jwt.go#L276)）；refresh 轮换用 `SetNX` 原子抢占并区分「抢占失败」与「Redis 出错」；固定签名算法 + 强制 exp 防 alg 降级与 nil 解引用。这些都是高质量的防御性实现。
- **雪花算法**：时钟回拨 ≤5ms 等待追平、>5ms 返回 error 降级而非 panic。
- **kafka 消费**：毒消息重试上限后跳过提交，避免死循环；`ctx` 取消时不提交 offset 让重投。
- **分页**：游标分页 + 深分页 JOIN 子查询优化，且有「排序必须按 id 否则游标重复/漏行」的注释提醒。
- **CSRF**：HTTP 明文环境跳过 Referer 强校验，HTTPS 保留完整校验；失败时 Abort 防双写。
- **broker 多进程**：supervisor + 子进程 + `done` channel 可靠等待，避免依赖 `cmdExit` 投递时序。

---

## 七、执行结果（2026-08-15）

按本报告优先级执行，两个模板 `go build` / `go vet` / `go test` 全部通过。

### 已修复
| 项 | 改动 |
|---|---|
| **#1 ClientIP 伪造** | `utils.ClientIP` 改为直接 `return c.ClientIP()`，删除手动解析 `X-Real-IP`/`X-Forwarded-For` 的可伪造逻辑（basic & advanced）。 |
| **#3 kafka Publisher race** | `Publisher` 去掉 `sync.Once` 懒加载，构造期即创建 Writer，`Close` 不再与并发 Publish 竞争（advanced）。 |
| **#7 pkg→internal 解耦** | 新增 [internal/app](tpl/advanced/internal/app/)（basic/advanced）承接 gin 引擎装配（中间件/JWT/限流/路由/Swagger）与资源关闭器注册；`pkg/xserver/http` 只做 HTTP 生命周期。broker 的健康探针与子进程入口也上移至 `internal/app`；事件订阅配置类型提取到 [pkg/xbroker](tpl/advanced/pkg/xbroker/)（仅依赖 pkg/asynq）。现在 `pkg/` 不再 import 任何 `internal/*`。 |
| **#4 xlog 全局 closer** | `kafkaClosers` 改为受互斥锁保护的切片，多实例（broker 父子进程）各自追加、`Close` 统一关闭，不再互相覆盖（basic & advanced）。 |
| **#5 Stop 二次关闭** | `http.Server.Stop` 用 `sync.Once` 守卫，监听失败与信号退出只执行一次完整回收（basic & advanced）。 |
| **#8 gin 全局态** | 新增 `xserver.InitGinMode`，http/broker health 统一调用，消除 3 处重复设置。 |
| **#9 Stop 序列抽取** | 新增 `xserver.Closer`（LIFO 注册/关闭），http 与 broker 的资源回收（db/redis/kafka/tracer/xlog）统一走 Closer，新增资源只需注册一次。 |
| **#2 OrderType 注释** | basic 注释改为 `0:降序(默认),1:升序`，与 advanced 及 `HandleRank` 实际行为一致。 |
| **#11 行尾统一** | basic 补 `.gitattributes`（沿用 advanced 的 `*.* text eol=lf`），14 个 CRLF `.go` 文件与 `basic.sql` 归一化为 LF；已确认这些文件除行尾外无实质内容变化。 |
| **#13 死错误** | `SonySnowFlake()` 改为返回 `(uint64, error)`，使用 `ErrSonyflakeNotInit`，不再 panic（advanced）。 |
| **#16 注释噪声** | 删除 `event/publisher.go` 末尾约 75 行注释掉的演示代码。 |
| **#17 Get footgun** | 热重载时新快照 `current` 自指，任意 `Get()` 返回的快照再调 `Get()` 不再拿到 nil（basic & advanced）。 |

### 未改动（有意保留）
- **#6 限流 TOCTOU**：当前语义（封禁后计数清零）可接受，报告亦标注「非必须改」。
- **#10 demo 去重**：advanced 是 basic 的超集（RoleId/Password/事件/流式），属合理功能差异；模板生成器层面的单一来源留待 `kun` 生成器改造，短期已手工对齐注释。
- **#14 错误码约定**：`NewError(ctx, code, msg, nil)` 不产错误码属于既有约定，未改动以免影响调用方。
- **#15 SendMsg 演示重复**：属教学示例，报告标注「优先级低」。

### 附注
- wire 重新生成时 `go.sum` 新增 `github.com/google/subcommands`、`golang.org/x/mod` 校验和条目，系 wire 工具运行期依赖，非项目运行依赖，保留以便 wire 可复现。
