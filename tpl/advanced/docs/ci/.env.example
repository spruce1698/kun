# 复制为 .env 后修改(.env 已被 .gitignore 忽略,不会提交)。
#   cp .env.example .env
#
# 这些口令供本地开发的 docker compose 使用,须与应用 config/local.yml
# 里的 mysql.source / redis.password 保持一致,否则应用连不上依赖。
MYSQL_ROOT_PASSWORD=change-me-local-only
MYSQL_DATABASE=advanced
REDIS_PASSWORD=change-me-local-only
