调度器:管理事件和监听器之间的关系，并负责触发事件。
异步消息,延时消息,周期性任务,消费处理,job任务

服务器(Broker)：
作为消息中转站，接收来自发布者的消息并将它们分发给相应的订阅者。

发布者(Publisher,publisher)： producer             -> Pub
发送数据的实体，可以向任意主题(topic)发布消息。         -> Sub

订阅者(Subscriber)： Sub
接收数据的实体，它可以订阅一个或多个主题，并在相关主题上有新消息时收到通知。